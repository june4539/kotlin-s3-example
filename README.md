# `Spring Kotlin AWS S3`

```yaml
cloud:
  aws:
    credentials:
      accessKey: {accessKey}
      secretKey: {secretKey}
    s3:
      bucket: {bucket}
      dir: images/
    region:
      static: ap-northeast-2
    stack:
      auto: false

```
